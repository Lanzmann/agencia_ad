import styled from "styled-components";

const Img = styled.img`
    width: 1.75rem; // adjust as needed
    height: 1.75rem; // adjust as needed
`;

const TomatoIcon = () => (
    <Img src="icons/tomatoIcon_m.png" alt="Tomato Icon" />
);

export default TomatoIcon;